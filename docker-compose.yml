web:
  build: .
  environment:
    MYENV: FindMeServ
  volumes:
    - .:/app
  restart: always 
web_migrate:
  extends:
    service: web
  command: python manage.py migrate
web_run:
  extends:
    service: web
  command: python manage.py runserver 0.0.0.0:80
  ports:
    - "80:80"